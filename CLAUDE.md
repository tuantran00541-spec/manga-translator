# Working rules

- Bench and audit output goes to the `audit-evidence` branch (`.github/actions/save-evidence`), never to code branches or main.
- Delete one-off benches, scripts and options once they have answered their question.
- Comments and docstrings: one short sentence.
