Validation targets for this pass:

1. Production Python source compiles.
2. All production JavaScript passes `node --check`.
3. Fixed LaMa remains on the locked compatibility path.
4. Shared seam scheduling still spans each backend request.
5. Completed pages are promoted before the slowest page finishes.
6. Request-size enforcement rejects both oversized Content-Length and streamed bodies.
