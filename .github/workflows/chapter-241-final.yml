name: Chapter 241 Final Layer One

on:
  push:
    branches:
      - 'chapter/doctors-rebirth\+241'
    paths:
      - '.github/workflows/chapter-241-final.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: chapter-241-human-loop
  cancel-in-progress: false

jobs:
  final-package:
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    env:
      GH_TOKEN: ${{ github.token }}
      RENDER_RUN_ID: '34744936501'
      RENDER_ARTIFACT: 05-render-candidate-c241d001
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 'chapter/doctors-rebirth+241'
      - name: Layer one
        run: echo FINAL_LAYER_ONE_PASS
