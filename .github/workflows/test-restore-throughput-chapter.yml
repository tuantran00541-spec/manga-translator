name: Restore throughput real chapter test

on:
  push:
    branches:
      - test/restore-throughput-doctors-rebirth-241
    paths:
      - .github/workflows/test-restore-throughput-chapter.yml

permissions:
  contents: read

concurrency:
  group: restore-throughput-real-chapter
  cancel-in-progress: false

jobs:
  real-chapter:
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    env:
      PYTHONUNBUFFERED: "1"
      MANGA_USE_DYNAMIC_LAMA: "1"
      MANGA_ORT_PROVIDER: openvino
      MANGA_ORT_REQUIRE_PROVIDER: "1"
      MANGA_ORT_OPENVINO_SCOPE: detectors
      MANGA_ORT_OPENVINO_THREADS: "2"
      MANGA_ORT_OPENVINO_STREAMS: "1"
      MANGA_ORT_OPENVINO_CACHE_DIR: /tmp/manga-openvino-cache
      OPENBLAS_NUM_THREADS: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install production processing runtime
        run: |
          python -m pip install \
            numpy==1.26.4 Pillow==10.4.0 opencv-contrib-python==4.10.0.84 \
            onnxruntime-openvino==1.24.1 psutil==6.1.1 loguru==0.7.2 \
            requests==2.32.3 beautifulsoup4==4.12.3 lxml==5.3.0 \
            playwright==1.47.0 fastapi==0.115.0 pydantic==2.9.2 \
            filelock==3.16.1 threadpoolctl==3.6.0
      - name: Restore detector and inpaint models
        id: models
        uses: actions/cache@v4
        with:
          path: |
            models/*.onnx
            models/.e2e-provenance.json
          key: processing-profile-models-b224548-v1
      - name: Read pinned model provisioner
        if: steps.models.outputs.cache-hit != 'true'
        uses: actions/checkout@v4
        with:
          ref: b22454827f8190655d7070b80eb20c2550beadfa
          path: benchmark-bootstrap
          sparse-checkout: scripts/provision_real_e2e_models.py
          sparse-checkout-cone-mode: false
      - name: Provision public-equivalent production models
        if: steps.models.outputs.cache-hit != 'true'
        run: |
          python -m pip install --index-url https://download.pytorch.org/whl/cpu --extra-index-url https://pypi.org/simple torch==2.5.1+cpu torchvision==0.20.1+cpu
          python -m pip install --no-deps ultralytics==8.3.221 onnx==1.17.0 onnxslim==0.1.71
          python -m pip install numpy==1.26.4 sympy==1.13.1 PyYAML==6.0.2 scipy==1.14.1 tqdm==4.67.1 matplotlib==3.9.2 pandas==2.2.3 py-cpuinfo==9.0.0 ml_dtypes==0.5.3 colorama==0.4.6 polars==1.33.1 ultralytics-thop==2.0.17
          python benchmark-bootstrap/scripts/provision_real_e2e_models.py
          cp benchmark-bootstrap/models/*.onnx models/
          cp benchmark-bootstrap/models/.e2e-provenance.json models/
      - name: Process one real chapter
        run: |
          mkdir -p benchmark-results/restore-throughput
          python - <<'PY'
          from __future__ import annotations
          import hashlib, json, os, threading, time
          from pathlib import Path
          import onnxruntime as ort
          import psutil

          from app.manifest_utils import load_manifest_raw
          from app.optimized_pipeline import OptimizedChapterPipeline
          from app.parameters import parameter_snapshot

          URL = 'https://asurascans.com/comics/killer-pietro-08677664/chapter/120'
          OUT = Path('benchmark-results/restore-throughput/report.json')
          process = psutil.Process()
          peak = {'rss_mb': process.memory_info().rss / 2**20}
          stop = threading.Event()

          def sample():
              while not stop.wait(0.10):
                  peak['rss_mb'] = max(peak['rss_mb'], process.memory_info().rss / 2**20)

          sampler = threading.Thread(target=sample, daemon=True)
          sampler.start()
          report = {
              'source_sha': os.getenv('GITHUB_SHA'),
              'chapter_url': URL,
              'workers_requested': 2,
              'onnxruntime': ort.__version__,
              'providers': ort.get_available_providers(),
              'parameters': parameter_snapshot(),
          }
          try:
              pipeline = OptimizedChapterPipeline()
              chapter_id = hashlib.sha256(f'restore-throughput-{time.time_ns()}'.encode()).hexdigest()[:8]
              report['chapter_id'] = chapter_id

              t0 = time.perf_counter()
              manifest = pipeline.download_chapter(URL, chapter_id, workers=2)
              report['ingest_s'] = round(time.perf_counter() - t0, 3)
              indices = list(range(len(manifest.get('pages', []))))
              report['slices'] = len(indices)
              report['source_pages'] = len({p.get('source_page') for p in manifest.get('pages', [])})
              if not indices:
                  raise RuntimeError('chapter import produced zero slices')

              t0 = time.perf_counter()
              pipeline.process_pages(chapter_id, indices, workers=2)
              wall = time.perf_counter() - t0
              report['process_wall_s'] = round(wall, 3)
              report['slices_per_min'] = round(len(indices) * 60.0 / wall, 3)
              report['ms_per_slice'] = round(wall * 1000.0 / len(indices), 3)

              manifest = load_manifest_raw(chapter_id)
              pages = manifest.get('pages', [])
              report['clean_pages'] = sum(bool(p.get('clean')) for p in pages)
              report['boxes'] = sum(len(p.get('boxes', [])) for p in pages)
              report['last_processing_run'] = manifest.get('last_processing_run')
              report['failed_page_indices'] = (manifest.get('last_processing_run') or {}).get('failed_page_indices') or []
              report['peak_rss_mb'] = round(peak['rss_mb'], 1)

              if report['clean_pages'] != len(pages):
                  raise RuntimeError(f"clean pages {report['clean_pages']} != slices {len(pages)}")
              if report['failed_page_indices']:
                  raise RuntimeError(f"processing failures: {report['failed_page_indices']}")
          finally:
              stop.set()
              sampler.join(timeout=1)
              report['peak_rss_mb'] = round(max(peak['rss_mb'], process.memory_info().rss / 2**20), 1)
              OUT.write_text(json.dumps(report, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')
              print('RESTORE_THROUGHPUT_RESULT=' + json.dumps(report, ensure_ascii=False), flush=True)
          PY
      - name: Publish summary
        if: always()
        run: |
          python - <<'PY'
          import json, os
          from pathlib import Path
          p = Path('benchmark-results/restore-throughput/report.json')
          if not p.is_file():
              raise SystemExit(0)
          r = json.loads(p.read_text(encoding='utf-8'))
          lines = [
              '## Restore throughput real chapter', '',
              f"- Source pages / slices: `{r.get('source_pages')}` / `{r.get('slices')}`",
              f"- Clean pages: `{r.get('clean_pages')}`",
              f"- Process wall: **{r.get('process_wall_s')} s**",
              f"- Throughput: **{r.get('slices_per_min')} slices/min** ({r.get('ms_per_slice')} ms/slice)",
              f"- Peak RSS: **{r.get('peak_rss_mb')} MiB**",
              f"- Failed pages: `{r.get('failed_page_indices')}`",
          ]
          with open(os.environ['GITHUB_STEP_SUMMARY'], 'a', encoding='utf-8') as out:
              out.write('\n'.join(lines) + '\n')
          print('\n'.join(lines))
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: restore-throughput-real-chapter-${{ github.run_id }}
          path: benchmark-results/restore-throughput/
          retention-days: 7
          if-no-files-found: warn
