name: Chapter 241 Import

on:
  push:
    branches:
      - '**'
    paths:
      - '.github/workflows/chapter-241-import.yml'

permissions:
  contents: write

jobs:
  import:
    if: github.ref == 'refs/heads/chapter/doctors-rebirth+241'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 'chapter/doctors-rebirth+241'
      - run: echo "chapter 241 import bootstrap v4"
